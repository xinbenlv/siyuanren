.hero-unit
    h1 请登录
    br
    p 非常简易的版本

    form.form-horizontal(ng-submit="login()", name="loginForm")
        .control-group
            label.control-label(for="username") Username
            .controls
                input(type="text", data-ng-model="username", placeholder="Username", name="username", required)
        .control-group
            label.control-label(for="password") Password
            .controls
                input(type="password", data-ng-model="password", placeholder="Password", name="password", required)
        .control-group
            .controls
                label(for="rememberme").checkbox
                    input(type="checkbox", data-ng-model="rememberme", name="rememberme", value="0")
                    | Remember me
        .control-group
            .controls
                button.btn(type="submit") Log in
    hr
    p ... or use one of them fancy social logins:
    .btn-group
        a.btn(href="", data-ng-click="loginOauth('facebook')")
            i.icon-facebook-sign
            |  Facebook
        a.btn(href="", data-ng-click="loginOauth('twitter')")
            i.icon-twitter
            |  Twitter
        a.btn(href="", data-ng-click="loginOauth('google')")
            i.icon-google-plus-sign
            |  Google
        a.btn(href="", data-ng-click="loginOauth('linkedin')")
            i.icon-linkedin
            |  LinkedIn
        a.btn(href="", data-ng-click="loginOauth('weibo')")
            i.icon-weibo
            | Weibo