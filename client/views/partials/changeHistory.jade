.container
    .large-empty-row
    h1 修订历史
    .small-empty-row
    div#loadingIndicator(data-ng-show="loading")
        img.centerHorizontal.centerVertical(src='/img/loadingIndicator.gif', alt='Loading...')
    table.table.table-striped.table-bordered(data-ng-show="!loading && histories.length")
        thead
            tr
                th 时间
                th 修改者
                th 被修改者
                th 变化
        tbody
            tr(data-ng-repeat="h in histories")
                td {{ h.timestamp }}
                td {{ h.Modifier }}
                td {{ h.Modifiee }}
                td {{ h.Change }}
    div(data-ng-show="!loading && !histories.length") 通讯录没有修改记录